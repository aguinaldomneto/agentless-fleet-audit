FROM rockylinux/rockylinux:9
RUN dnf install -y openssh-server openssl procps-ng \
 && dnf clean all \
 && useradd -m -s /bin/sh collector
COPY entrypoint.sh /entrypoint.sh
RUN chmod 755 /entrypoint.sh
EXPOSE 22
ENTRYPOINT ["/entrypoint.sh"]
